# Tar archives are meant for systemd-nspawn containers: they carry no kernel,
# so do not build the default initrd sub-image for them. Besides saving time,
# this keeps Open Build Service builds working, where only the packages listed
# in the recipe are available offline.
[Match]
Format=tar

[Content]
Initrds=
