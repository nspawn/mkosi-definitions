# mkosi writes the repositories it builds from into its sandbox, never into the image, so
# zypper inside the machine starts with none. The repos package of the release registers
# the openSUSE service that provides them.
[Match]
Release=tumbleweed

[Content]
Packages=
        openSUSE-repos-Tumbleweed
