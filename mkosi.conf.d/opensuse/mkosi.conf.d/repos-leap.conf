# The Leap counterpart of repos-tumbleweed.conf: openSUSE-repos-Leap is built per release
# (lp160 for 16.0) and registers the openSUSE service with that release's repositories.
[Match]
Release=!tumbleweed

[Content]
Packages=
        openSUSE-repos-Leap
