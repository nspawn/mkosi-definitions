[Match]
Distribution=kali

[Distribution]
Release=kali-rolling

[Content]
# A minimal Kali, like the official container image: apt and the base system, the tools
# are one `apt install kali-linux-headless` away.
Packages=
        apt
        gnupg2
        iproute2
        iputils-ping
        systemd
        systemd-resolved
        vim
