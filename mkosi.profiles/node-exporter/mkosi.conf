# Prometheus node exporter on the Debian image: mkosi -d debian --profile=node-exporter
[Output]
ImageId=node-exporter
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="Prometheus node exporter"
               org.opencontainers.image.description="Prometheus node exporter on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 9100 on every address. Most of what it reports is the host's kernel view (CPU, memory, disks), which is what a monitoring machine wants. Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="Prometheus node exporter"
          org.opencontainers.image.description="Prometheus node exporter on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 9100 on every address. Most of what it reports is the host's kernel view (CPU, memory, disks), which is what a monitoring machine wants. Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        prometheus-node-exporter
