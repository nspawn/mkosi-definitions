# Redis on the Debian image: mkosi -d debian --profile=redis
[Output]
ImageId=redis
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="Redis"
               org.opencontainers.image.description="Redis on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 6379 on every address (no password until you set requirepass), data in /var/lib/redis and configuration in /etc/redis. Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="Redis"
          org.opencontainers.image.description="Redis on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 6379 on every address (no password until you set requirepass), data in /var/lib/redis and configuration in /etc/redis. Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        redis-server
