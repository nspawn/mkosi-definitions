# Python on the Debian image: mkosi -d debian --profile=python
[Output]
ImageId=python
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="Python"
               org.opencontainers.image.description="Python %r on Debian as a systemd-nspawn machine: the distribution's interpreter with pip, venv and a compiler, for a development machine you keep (-v ~/src:/root/src). Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="Python"
          org.opencontainers.image.description="Python %r on Debian as a systemd-nspawn machine: the distribution's interpreter with pip, venv and a compiler, for a development machine you keep (-v ~/src:/root/src). Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        build-essential
        ca-certificates
        curl
        git
        python3
        python3-dev
        python3-pip
        python3-venv
        vim
