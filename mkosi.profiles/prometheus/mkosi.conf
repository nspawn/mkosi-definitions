# Prometheus on the Debian image: mkosi -d debian --profile=prometheus
[Output]
ImageId=prometheus
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="Prometheus"
               org.opencontainers.image.description="Prometheus on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 9090 on every address, scraping itself by default, data in /var/lib/prometheus and configuration in /etc/prometheus/prometheus.yml. Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="Prometheus"
          org.opencontainers.image.description="Prometheus on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 9090 on every address, scraping itself by default, data in /var/lib/prometheus and configuration in /etc/prometheus/prometheus.yml. Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        prometheus
