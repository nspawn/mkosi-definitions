# Samba on the Debian image: mkosi -d debian --profile=samba
[Output]
ImageId=samba
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="Samba"
               org.opencontainers.image.description="Samba (SMB file server) on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 445, one guest-writable share named data on /srv/samba/data, configuration in /etc/samba/smb.conf. Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="Samba"
          org.opencontainers.image.description="Samba (SMB file server) on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 445, one guest-writable share named data on /srv/samba/data, configuration in /etc/samba/smb.conf. Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        samba
