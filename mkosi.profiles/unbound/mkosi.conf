# Unbound on the Debian image: mkosi -d debian --profile=unbound
[Output]
ImageId=unbound
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="Unbound"
               org.opencontainers.image.description="Unbound (recursive DNS resolver) on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 53 on every address, answering private networks, configuration in /etc/unbound/unbound.conf.d. Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="Unbound"
          org.opencontainers.image.description="Unbound (recursive DNS resolver) on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 53 on every address, answering private networks, configuration in /etc/unbound/unbound.conf.d. Built with mkosi from nspawn/mkosi-definitions."

[Content]
# dns-root-data brings the DNSSEC root trust anchor unbound refuses to start without; it is
# only a Recommends, and mkosi installs none of those.
Packages=
        unbound
        dns-root-data
