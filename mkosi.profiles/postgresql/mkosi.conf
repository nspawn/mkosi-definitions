# PostgreSQL on the Debian image: mkosi -d debian --profile=postgresql
[Output]
ImageId=postgresql
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="PostgreSQL"
               org.opencontainers.image.description="PostgreSQL on Debian %r as a systemd-nspawn machine: the distribution's package with its default cluster, enabled as a service, data in /var/lib/postgresql and configuration in /etc/postgresql. Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="PostgreSQL"
          org.opencontainers.image.description="PostgreSQL on Debian %r as a systemd-nspawn machine: the distribution's package with its default cluster, enabled as a service, data in /var/lib/postgresql and configuration in /etc/postgresql. Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        postgresql
