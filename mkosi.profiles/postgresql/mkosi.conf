# PostgreSQL on the Debian image: mkosi -d debian --profile=postgresql
[Output]
ImageId=postgresql
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="PostgreSQL"
               org.opencontainers.image.description="PostgreSQL on Debian %r as a systemd-nspawn machine: the distribution's package with its default cluster, enabled as a service on port 5432 on every address, password authentication from the network (set one with ALTER USER first), data in /var/lib/postgresql and configuration in /etc/postgresql. Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="PostgreSQL"
          org.opencontainers.image.description="PostgreSQL on Debian %r as a systemd-nspawn machine: the distribution's package with its default cluster, enabled as a service on port 5432 on every address, password authentication from the network (set one with ALTER USER first), data in /var/lib/postgresql and configuration in /etc/postgresql. Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        postgresql
