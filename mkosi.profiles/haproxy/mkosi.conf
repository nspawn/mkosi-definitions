# HAProxy on the Debian image: mkosi -d debian --profile=haproxy
[Output]
ImageId=haproxy
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="HAProxy"
               org.opencontainers.image.description="HAProxy on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service, with its configuration in /etc/haproxy/haproxy.cfg (mount yours: the default one defines no frontend). Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="HAProxy"
          org.opencontainers.image.description="HAProxy on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service, with its configuration in /etc/haproxy/haproxy.cfg (mount yours: the default one defines no frontend). Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        haproxy
