# dnsmasq on the Debian image: mkosi -d debian --profile=dnsmasq
[Output]
ImageId=dnsmasq
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="dnsmasq"
               org.opencontainers.image.description="dnsmasq (DNS forwarder and DHCP server) on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 53 on every address, forwarding to the upstream servers of the machine, configuration in /etc/dnsmasq.d. Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="dnsmasq"
          org.opencontainers.image.description="dnsmasq (DNS forwarder and DHCP server) on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service on port 53 on every address, forwarding to the upstream servers of the machine, configuration in /etc/dnsmasq.d. Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        dnsmasq
        dns-root-data
