# OpenJDK on the Debian image: mkosi -d debian --profile=openjdk
[Output]
ImageId=openjdk
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="OpenJDK"
               org.opencontainers.image.description="OpenJDK %r on Debian as a systemd-nspawn machine: the distribution's JDK with Maven, for a development machine you keep (-v ~/src:/root/src). Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="OpenJDK"
          org.opencontainers.image.description="OpenJDK %r on Debian as a systemd-nspawn machine: the distribution's JDK with Maven, for a development machine you keep (-v ~/src:/root/src). Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        ca-certificates
        curl
        git
        maven
        openjdk-21-jdk
        vim
