# Apache HTTP Server on the Debian image: mkosi -d debian --profile=apache
[Output]
ImageId=apache
Output=%i_%v_%a
OciAnnotations=org.opencontainers.image.title="Apache HTTP Server"
               org.opencontainers.image.description="Apache HTTP Server on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service, serving /var/www/html on port 80 with its configuration in /etc/apache2. Built with mkosi from nspawn/mkosi-definitions."
OciLabels=org.opencontainers.image.title="Apache HTTP Server"
          org.opencontainers.image.description="Apache HTTP Server on Debian %r as a systemd-nspawn machine: the distribution's package, enabled as a service, serving /var/www/html on port 80 with its configuration in /etc/apache2. Built with mkosi from nspawn/mkosi-definitions."

[Content]
Packages=
        apache2
